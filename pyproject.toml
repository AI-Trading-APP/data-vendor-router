[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "data-vendor-router"
version = "0.1.4"
description = "Category-routed data-fetch with automatic per-vendor fallback (yfinance, Alpaca, Benzinga, Polygon, Alpha Vantage, NewsAPI, Tiingo, OpenBB)"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "AI-Trading-APP", email = "kasi@ascendsoft.co" }]
dependencies = [
    "pydantic>=2.6,<3.0",
    "httpx>=0.28",
    "pybreaker>=1.0",
    "opentelemetry-api>=1.25",
    "prometheus-client>=0.20",
]

[project.optional-dependencies]
vendors = [
    "yfinance>=0.2.40",
    "alpaca-py>=0.35",
]
openbb = [
    # Umbrella package required: it is versioned 4.x and triggers the SDK build step
    # that wires providers into obb.equity.* etc. Provider sub-packages are 1.x (not 4.x).
    "openbb>=4.7,<5.0",
    "openbb-core>=1.6,<2.0",
    "openbb-fmp>=1.6,<2.0",
    "openbb-polygon>=1.5,<2.0",
    "openbb-sec>=1.6,<2.0",
    "openbb-yfinance>=1.6,<2.0",
    # P2 (add when build-out lands):
    # "openbb-fred>=1.6,<2.0",
]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "pytest-asyncio>=0.23",
    "opentelemetry-sdk>=1.25",
    "ruff",
    "mypy>=1.10",
]

[tool.hatch.build.targets.wheel]
packages = ["src/data_vendor_router"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "live_vendor: tests that hit real vendor APIs (excluded from default run)",
]
addopts = "-m 'not live_vendor'"
